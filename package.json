{
  "name": "@company/base-robot-mobile-automation-test",
  "version": "1.7.11",
  "description": "Projeto base para construção de testes automatizados mobile com robot",
  "main": "lib/index.js",
  "author": "hefesto@company.com.br",
  "engines": {
    "node": "18.x",
    "npm": ">=6.0.0"
  },
  "files": [
    "src/**/*.py",
    "src/**/**/*.py",
    "src/**/*.robot",
    "src/**/**/*.robot"
  ],
  "scripts": {
    "build": "npm run robot",
    "setup": "pip install -r requirements.txt",
    "robot": "copyfiles src/**/*.robot ./dist",
    "py": "copyfiles src/**/*.py ./dist"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1"
  },
  "dependencies": {
   
  }
}